.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/

# Scratch output from test-driving the template (the README uses `copier copy … ./myproj`).
/myproj/

# Agent worktrees/state. Nested git repos here also break `vcs_ref="HEAD"` renders
# (copier's `git add -A` stages them as phantom submodules → exit 128).
/.claude/
