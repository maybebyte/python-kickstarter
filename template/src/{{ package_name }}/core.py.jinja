"""Core logic for {{ project_name | replace('\\', '\\\\') | replace('"', '\\"') }}."""


def clamp(value: int, low: int, high: int) -> int:
    """Constrain ``value`` to the inclusive range ``[low, high]``."""
    if value < low:
        return low
    if value > high:
        return high
    return value
